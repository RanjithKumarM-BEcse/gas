
  # LPG Gas Detection App

  This is a code bundle for LPG Gas Detection App. The original project is available at https://www.figma.com/design/ihsD1jthyvsGLPSi7XAZye/LPG-Gas-Detection-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  "# gas" 
